// Assignment 1 - CMSC 21 Lecture

#include <stdio.h>

int main(void){ //'V' should be in lowercase

    int sum; //'int' should be in lowercase

    //COMPUTE RESULT - single line comment should be (//)

    sum = 25 + 37 - 19; //should end the statement with a semicolon (;)
    // DISPLAY RESULTS - single line comment should be (//) and must not end with a comment syntax (//)

    printf("The answer is %i\n", sum); //should have a comma indiccating the value of %i
    return 0;
}
